#include <stdio.h>

int main(void)
{
    //Loacl variable definition
    int a =10;

    //Check Boolean conditions using the if statement
    if( a < 20 )
    {
        //If the condition is true,the following statement is printed
        printf("a is less than 20\n");
    }
    printf("The value of a is %d\n",a);

    return 0;
}